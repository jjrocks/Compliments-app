<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
        <!--<item name="android:actionBarStyle">@style/ThemeActionBar</item>-->
        <!--<item name="android:windowActionBarOverlay">true</item>-->
        <!-- Compaitibility -->
        <item name="windowTranslucentStatus">true</item>
        <!--<item name="actionBarStyle">@style/ThemeActionBar</item>-->
        <!--<item name="windowActionBarOverlay">true</item>-->
    </style>

    <style name="ThemeActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:background">@color/transperent_color</item>
        <item name="android:elevation">4dp</item>
        <item name="background">@color/transperent_color</item>
        <item name="elevation">4dp</item>
    </style>

    <style name="about_text">
        <item name="android:textColor">@color/about_text_color</item>
        <item name="android:textColorLink">@color/about_link_color</item>
    </style>
</resources>
